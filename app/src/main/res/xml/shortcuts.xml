<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_camera_enhance_black_24dp"
        android:shortcutDisabledMessage="@string/take_picture_disabled_message1"
        android:shortcutId="createGif"
        android:shortcutLongLabel="@string/take_picture_shortcut_long_label1"
        android:shortcutShortLabel="@string/take_picture_shortcut_short_label1">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="com.sthagios.stopmotion.create.CreateNewImageActivity"
            android:targetPackage="com.sthagios.stopmotion"/>
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation"/>
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>
